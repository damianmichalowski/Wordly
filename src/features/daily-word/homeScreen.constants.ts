/**
 * `false` — Daily Word without `ScreenHeader`, safe-area padding only; no track pill.
 * Set `true` to restore header + `TrackProgressPill`.
 */
export const HOME_SHOW_DAILY_HEADER_AND_TRACK_PILL = false;
