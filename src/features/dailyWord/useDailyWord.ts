export {
  useCurrentWord,
  type UseCurrentWordReturn,
} from "@/src/hooks/useCurrentWord";

/** @deprecated Use {@link useCurrentWord} from `@/src/hooks/useCurrentWord`. */
export { useCurrentWord as useDailyWord } from "@/src/hooks/useCurrentWord";

export type { UseCurrentWordReturn as UseDailyWordReturn } from "@/src/hooks/useCurrentWord";
